import { Title, Text } from '@mantine/core';

const ReservationsList: React.FC = () => {
  return (
    <div>
      <Title order={1} mb="md">Stock Reservations</Title>
      <Text c="dimmed">Stock reservations component coming soon...</Text>
    </div>
  );
};

export default ReservationsList;