import { Title, Text } from '@mantine/core';

const CommittedStockList: React.FC = () => {
  return (
    <div>
      <Title order={1} mb="md">Committed Stock</Title>
      <Text c="dimmed">Committed stock component coming soon...</Text>
    </div>
  );
};

export default CommittedStockList;